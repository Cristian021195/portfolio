//export const url = `${window.location.protocol}//${window.location.host}/`
export class Rutas{
    constructor(){
        this.principal = `${window.location.protocol}//${window.location.host}/`;
        this.publica = `${window.location.protocol}//${window.location.host}/portfolio/public/`;
        this.publica_local = `${window.location.protocol}//${window.location.host}/public/`;
    }
}